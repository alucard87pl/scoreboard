/** Quiz configuration file
 * All values must be set before running the application
 * Text values must be in double quotes "like this"
 * Numbers are written directly without quotes
 */

/** Name/title of the quiz that appears in the header */
quizBranding = "Leniwcowy SuperPubQuiz"

/** Location where the quiz takes place
 * Displayed in the header below the title */
quizLocation = "Sosnowiec"

/** Name of the quiz host/game master
 * Displayed in the header below location */
quizHost = "Pinat"

/** Number of scoring categories/rounds in the quiz 
 * can safely handle up to 16, but a more feasible limit is 10.
*/
numberOfCategories = 14

/** Enable/disable the Lucky Loser functionality
 * When true, shows additional column and controls */
includeLuckyLoser = true

/**
 * Maximum number of points that can be achieved per category
 */
maxScore = 5